# Machine-Learning-Combo
Naive Bayes custom implementation for a combined dataset of categorical and numeric features. Improving the baseline algorithm with at least 11% better accuracy depending on the seed
